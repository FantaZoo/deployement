all:
  vars:
    ansible_user: root
  children:
    group1:
      hosts:
        # Configured on https://www.duckdns.org
        fantazoo.duckdns.org:
      vars:
        domain_name: fantazoo.duckdns.org
          
